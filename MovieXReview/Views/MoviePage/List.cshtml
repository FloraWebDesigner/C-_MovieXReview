@model IEnumerable<MovieXReview.Models.MovieDto>

@{
    ViewData["Title"] = "List";
}

<div class="container-fluid bg-dark p-3 text-white">
    <h1 class="text-center">Movie List</h1>
</div>
<div class="container-fluid bg-warning p-3 text-dark">
    <div class="container">
<div class="d-flex flex-row justify-content-between mb-3">
    <a asp-action="New" class="btn btn-outline-dark shadow">Create New</a>
            <a href="/ViewerPage/List" class="btn btn-outline-dark shadow">Go to Viewer Overview</a>
            <a href="/TicketPage/List" class="btn btn-outline-dark shadow">Go to Booking Overview</a>
</div>
        <table class="table table-dark table-striped mb-5">
            <thead>
                <tr class="table-dark text-capitalize">
                    <th>
                        Poster
                    </th>
                    <th>
                @Html.DisplayNameFor(model => model.MovieName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Year)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Introduction)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Duration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Director)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Star)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TicketQuantity)
            </th>
            <th>
                Operation
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
                   <tr>
                        <td>
                           @*  @if (item.HasPic)
                            {
                                <img src="@item.MovieImgPath" style="width:6rem; height:10rem; object-fit: cover;" alt=" This is the movie @item.MovieName poster, image path = @item.MovieImgPath."  />
                            }
                            else{
                            <img src="~/img/movies/placeholder.jpg" style="width:6rem; height:10rem; object-fit: cover;" alt="This is the image placeholder, we haven't uploaded any poster yet, will update soon." />
                            }
                             *@
                        </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MovieName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Year)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Introduction)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Rate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Duration)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Director)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Star)
                </td>
                <td>
                            @Html.DisplayFor(modelItem => item.TicketQuantity)
                </td>
                        <td style="width: 15rem;">
                            <a asp-action="Edit" asp-route-id="@item.MovieId" class="btn btn-warning w-75 mb-sm-1 py-md-1">Edit</a>
                            <a asp-action="Details" asp-route-id="@item.MovieId" class="btn btn-warning w-75 mb-sm-1 py-md-1">Details</a>
                            <a asp-action="ConfirmDelete" asp-route-id="@item.MovieId" class="btn btn-warning w-75 py-md-1">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>
    </div>